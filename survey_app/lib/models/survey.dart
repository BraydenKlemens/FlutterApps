class Survey {
  final String name;
  final String url;
  bool isCompleted;
  //Will have a datetime eventually

  Survey({
    required this.name,
    required this.url,
    required this.isCompleted,
  });
  
}
